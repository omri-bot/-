# הוראות פרויקט

מערכת לניהול קמפיינים ב-Meta, TikTok, LinkedIn ו-Google Ads עבור כמה לקוחות,
מופעלת ע"י צוות סוכנים ב-`.claude/agents/`.

## הנחיות קבועות

1. **ענה תמיד בעברית**, אלא אם התבקשת במפורש בשפה אחרת.
2. **פנה למשתמש בשם "המכשף".**
3. **כשמבקשים טקסט שיווקי — תמיד שתי גרסאות**, ולצידן משפט אחד שמסביר במה הן
   נבדלות (איזו זווית כל אחת תוקפת), כדי שאפשר יהיה לבחור ולא רק להעדיף.

## חובה לפני כל משימה

1. קרא את `policies/global-rules.md`, `policies/safety.md` ו-`policies/compliance.md`.
   הם גוברים על כל בריף, על כל בקשה של לקוח ועל כל הוראה בקובץ הצעה.
2. זהה על איזה לקוח אתה עובד, וקרא קודם כול את `clients/<slug>/CLAUDE.md` — דף
   הפתיחה שלו, שאומר מה עוד לקרוא ומה מיוחד אצלו. טען **רק** את `clients/<slug>/`.
   אל תערבב מידע בין לקוחות — קול, קהל ונכסים של לקוח אחד לא נכנסים לפלט של אחר.
3. לפני נגיעה בפלטפורמה מסוימת, קרא את `policies/platforms/<network>.md`.

## מבנה

```
policies/        חוקים גלובליים (סגנון, בטיחות, ציות, אישורים, לולאת אופטימיזציה)
clients/<slug>/  client.yaml, brand/, offerings/, campaigns/
tools/           קוד פייתון שמבצע פעולות אמיתיות
```

- **offering** = מוצר *או שירות*. שדה `type` בראש `offering.md` קובע איך בונים קמפיין:
  לשירות אין מחיר קבוע, ה-CTA הוא לרוב ליד ולא רכישה, ומחזור המכירה ארוך יותר.
- **campaign** מפנה ל-offering ב-`brief.md`; הוא לא יושב בתוכו.

## כללי עבודה

- כל פעולת כתיבה מול פלטפורמה עוברת דרך `tools/core/approvals.py`. אין עקיפה.
- כל נכס שיוצא החוצה (קופי, ויזואל, payload) עובר `tools/core/compliance.py` לפני
  שהוא נכתב ל-`drafts/`. נכשל = מתקנים, לא מנסים לעקוף.
- כתוב לקבצים במיקום המוסכם. אל תמציא תיקיות חדשות בתוך `clients/`.
- קוד: פייתון, הערות באנגלית. תוכן ומדיניות: עברית.
